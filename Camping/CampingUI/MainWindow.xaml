<Window x:Class="CampingUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CampingUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowState="Maximized"
        WindowStyle="ThreeDBorderWindow"
        ResizeMode="CanMinimize"
        
        
        >
    <Grid>
        <!-- This shows the navigation bar for all pages in the WPF application -->
        <StackPanel Orientation="Horizontal" Height="75" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Button x:Name="ReservationsButton"  Content="Reserveringen" Width="200" Height="50" FontSize="20" Margin="0, 0, 12.5, 0" Click="ReservationsButton_Click"/>
            <Button x:Name="PlacesButton" Content="Plaatsen" Width="200" Height="50" FontSize="20" Margin="12.5, 0, 0, 0" Click="PlacesButton_Click"/>
        </StackPanel>
        <!-- In this frame the pages will show up -->
        <Frame x:Name="Main" Margin="0, 75, 0, 0" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
